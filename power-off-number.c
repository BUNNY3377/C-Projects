#include<stdio.h>
#include<math.h>
void main()
{
    double base = 2;
    double exponent = 3;
    double result = pow(base,exponent);

    printf("\n base is %lf exponent is %lf result is %lf",base,exponent,result);
}
