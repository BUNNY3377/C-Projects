#include<stdio.h>
#include<math.h>
void main()
{
    double dividend = 25;
    double divisor = 5;
    double result =fmod(dividend,divisor);

     printf("\n dividend is %lf divisore is %lf result is %lf",dividend,divisor,result);
}
